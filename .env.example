# Password hash for access gate
# Generate a SHA-256 hash of your password and place it here
# To generate a hash, you can use an online SHA-256 generator
# or run this in Node.js: crypto.createHash('sha256').update('your-password').digest('hex')
VITE_PASSWORD_HASH=your-sha256-hash-here
